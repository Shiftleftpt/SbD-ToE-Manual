"""Fixtures and utilities for RAG tools tagging tests

This conftest.py is SPECIFIC TO TAGGING CONTEXT.
It should NOT use RAG core fixtures or vice versa.

Location: tests/tagging/conftest.py
Context: RAG Tools (tagging workflows, auto-tagger)
Isolation: Separate fixtures from rag_core tests
"""

import pytest
